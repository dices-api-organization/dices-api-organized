const send = document.querySelector('.submit') as HTMLInputElement;
const user = document.querySelector('.name') as HTMLInputElement;
const pass = document.querySelector('pass') as HTMLInputElement;

send.addEventListener('click', (e) => {
  e.preventDefault();
 /*  if (user.value && pass.value) {

    } */
});
